# Frontend — Secure Authentication System (Made by MKCamara)

This is a Vite + React + Tailwind frontend.

To run:
1. cd frontend
2. npm install
3. npm run dev

Configure VITE_API_URL in an .env file if your backend is not the default.
